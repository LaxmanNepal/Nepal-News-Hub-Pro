(()=>{'use strict';const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const age=d=>{const m=Math.floor((Date.now()-new Date(d))/60000);return !Number.isFinite(m)||m<0?'अहिले':m<60?m+' मिनेट अघि':m<1440?Math.floor(m/60)+' घण्टा अघि':Math.floor(m/1440)+' दिन अघि'};const id=decodeURIComponent(new URLSearchParams(location.search).get('id')||'');async function run(){try{const r=await fetch('./news.json?ts='+Date.now(),{cache:'no-store'});const data=await r.json(),items=Array.isArray(data.items)?data.items:[];const matches=items.filter(x=>String(x.storyId||'')===id);if(!matches.length){$('#story').innerHTML='<div class="empty glass"><h2>स्टोरी भेटिएन</h2><p>यो स्टोरी पुरानो हुन सक्छ वा नयाँ इन्जिनले पुन: समूहबद्ध गरेको हुन सक्छ।</p></div>';return}matches.sort((a,b)=>new Date(a.pubDate)-new Date(b.pubDate));const h=matches[matches.length-1],first=matches[0],sources=[...new Map(matches.map(x=>[x.source,x])).values()];document.title=(h.title||'Story Intelligence')+' — नेपाल हब';$('#story').innerHTML=`<article class="hero glass"><img src="${esc(h.thumb||h.image||'https://images.unsplash.com/photo-1504711434969-e33886168f5c?w=1000&q=80')}" alt="${esc(h.title)}"><div class="hero-content"><small>${h.isBreaking?'🔴 ब्रेकिङ · ':''}📡 ${sources.length} स्रोत · ${age(h.pubDate)}</small><h1>${esc(h.title)}</h1><p>${esc(h.desc||'')}</p><a class="read" href="${esc(h.link)}" target="_blank" rel="noopener noreferrer">मूल समाचार पढ्नुहोस् ↗</a></div></article><section class="section"><div class="section-head"><div><h2>📊 Story Intelligence</h2><p>यो स्टोरी कति स्रोतमा र कहिले प्रकाशित भयो</p></div></div><div class="topic-grid"><div class="topic glass"><strong>${sources.length} स्रोत</strong><small>बहु-स्रोत कभरेज</small></div><div class="topic glass"><strong>${esc(age(first.pubDate))}</strong><small>सबैभन्दा पुरानो feed timestamp</small></div><div class="topic glass"><strong>${esc(age(h.pubDate))}</strong><small>सबैभन्दा नयाँ feed timestamp</small></div><div class="topic glass"><strong>${esc(h.storyCategory||h.category||'नेपाल')}</strong><small>मुख्य श्रेणी</small></div></div></section><section class="section"><div class="section-head"><div><h2>📰 स्रोत तुलना</h2><p>प्रत्येक publisher को headline, समय र मूल लिंक</p></div></div><div class="feed">${sources.map(x=>`<article class="card glass"><div class="card-body"><div class="meta">${esc(x.source||'समाचार')} · ${age(x.pubDate)}</div><h3>${esc(x.title)}</h3><p>${esc(x.desc||'')}</p><a class="read" href="${esc(x.link)}" target="_blank" rel="noopener noreferrer">मूल स्रोत खोल्नुहोस् ↗</a></div></article>`).join('')}</div></section><section class="section"><div class="section-head"><div><h2>🕐 Coverage Timeline</h2><p>feed मा देखिएको प्रकाशन क्रम</p></div></div><div class="feed">${matches.map(x=>`<article class="card glass"><div class="card-body"><div class="meta">${esc(x.source||'समाचार')}</div><h3>${esc(x.title)}</h3><div class="time">${new Date(x.pubDate).toLocaleString('ne-NP')}</div></div></article>`).join('')}</div></section></article>`}catch(e){$('#story').innerHTML='<div class="empty glass"><h2>लोड हुन सकेन</h2><p>केही समयपछि फेरि प्रयास गर्नुहोस्।</p></div>'}}run()})();